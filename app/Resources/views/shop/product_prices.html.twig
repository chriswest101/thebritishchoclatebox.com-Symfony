<p>
    <a class="item_add" href="{{ path('product', {prodCode: product.prodCode}) }}"><i></i> 
        {% if product.salePrice > 0.00 %}
            <span class="item_price item_price_strike">
                &pound;{{ product.price }}
            </span>
            <span class="item_price item_price_sale">
                &pound;{{ product.salePrice }}
            </span>                                    
        {% else %}
            <span class="item_price">
                &pound;{{ product.price }}
            </span>
        {% endif %}
    </a>
</p>